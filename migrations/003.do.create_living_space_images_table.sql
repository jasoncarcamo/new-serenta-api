CREATE TABLE living_space_images (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    url TEXT NOT NULL,
    living_space_id INTEGER REFERENCES living_space(id) ON DELETE CASCADE NOT NULL
);